# Python dependencies for Master_Automation scripts
# Install in the environment used by config/scripts.json "python_executable"
# e.g. pip install -r requirements.txt

# Pre-flight validation and Excel read (validate_exports.py)
# Summons backfill merge (summons_backfill_merge.py)
# Visual export normalizer (normalize_visual_export_for_backfill.py, summons/training_cost formats)
pandas>=1.3.0
openpyxl>=3.0.0
